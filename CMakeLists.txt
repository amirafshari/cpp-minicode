# CMakeLists.txt

# Set the minimum version of CMake required
cmake_minimum_required(VERSION 3.10)

# Set the project name
project(cpp-minicodes)

# Add the executable target
add_executable(isPrime isPrime.cpp)
